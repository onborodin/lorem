
import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state = function() {
    return {
    }
}

const mutations = {
}

const actions = {
}

const getters = {
}

export default new Vuex.Store({
    state: state,
    actions: actions,
    mutations: mutations,
    getters: getters
})
